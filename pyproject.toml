[project]
name = "srt-gen"
version = "0.5.0"
description = "Transcribe media files and generate a subtitle"
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
    "faster-whisper>=1.2.1",
    "mlx-audio>=0.4.5",
    "mlx-whisper>=0.4.3",
    "soundfile>=0.14.0",
]
